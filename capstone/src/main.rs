fn run() -> Result<(), String> {
    todo!("milestone 5: connect commands, model, and storage")
}
fn main() {
    if let Err(error) = run() {
        eprintln!("Error: {error}");
        std::process::exit(1);
    }
}
